# Tasks

## User

- Decide whether `www.continuoushq.com` and `www.getcontinuous.app` should serve the app; if yes, add those hostnames to `SITE_HOSTS` and rerun the domain deploy.

## Agent

- Continue adopting the canonical Core `worker.run.start` and `worker.run.complete` lifecycle gate inside registered worker command execution; Customer Experience `recovery.draft`, Growth `campaign.draft`, Owner `brief.generate`, Owner `decision_queue.prepare`, Owner `anomaly.triage`, Offer/Pricing `margin.review.prepare`, Finance `invoice.prepare`, Revenue `lead.read`, `lead.classify`, `response.draft`, `run`, `quote.prepare`, `payment_link.prepare`, and `continue`, Workforce `hire.packet.prepare` and `payroll_input.prepare`, Dispatch `schedule.propose` and `customer_update.draft`, Compliance `filing.prepare`, plus Systems `connector.health.scan`, `sync.repair.plan`, `data_quality.remediate`, `permission.review`, and `automation.plan` now use the Core run row and Core budget settlement, and replay repairs interrupted Core completions without leaking held budget, while Dispatch `closeout.prepare` and `exception.route`, plus remaining Finance commands still need their local run/budget/evidence writes migrated without double-counting.
- Extend the first Core-approved `customer_message.send` boundary beyond Revenue continuation receipt recording into a real provider executor only after production sender credentials, provider adapters, rollback playbooks, and reconciliation smoke tests exist; the current generic boundary validates approval proof, managed credential refs, scoped write grants, receipt capture, rollback/escalation proof, redacted output, and replay-bound `config.execution` without changing the `/worker` API shape.
- Keep Revenue readiness fail-closed through the first-class continuation, controlled-send receipt, payment-link continuation, adapter retry, and adapter reconciliation launch proof gates; keep payment-provider creation and money movement blocked until scoped provider credentials and rollback proof exist.
- Continue Growth beyond the first runtime `growth_operations` `command=campaign.draft` slice only after approval, source-claim proof, Core budget proof, scoped channel/ad/analytics credentials, adapter receipts, rollback evidence, and eval coverage are in place; publish/send/spend/tracking mutation stays blocked until then.
- Keep worker readiness and expansion status language aligned as worker families move; docs now define the full expansion status enum (`runtime`, `partial`, `planned_contract`, `candidate`, `packaged`), Asset/Supply is documented as a `candidate` catalog entry, and vertical packaged workers as `packaged` catalog bundles.
- Keep worker expansion promotion templates schema-shaped as new families are added; promotion payloads should stay on `/worker` with worker selectors in `worker` and operation inputs under command-owned `config` fields, not generic `requiredCoreRefs` bags or worker-family routes.
- Keep `continuous.worker.schema.apiShape`, `workerToolSchema.apiShape`, and the app-server worker manifest `apiShape` aligned whenever worker route, tool, selector, command, view, idempotency, or config placement changes; clients should consume that metadata rather than infer API shape from Revenue examples.
- Keep worker roadmap and package docs explicit that Core/platform readiness is the first launch gate: new workers should compose Core lifecycle, capability, budget, approval, evidence, app-server, AI gateway, and handoff primitives through generic envelopes before adding any runtime handler.
- Continue canonical workforce/filing/payment object coverage beyond the current Revenue runtime; `payment_link.prepare` now runs through `/worker` with invoice refs in `config` and blocked provider/money movement proof, entity setup now has a `locations` table plus Core `entity.setup.record`, worker identity/state now uses Core `worker.upsert` and `worker.transition`, payroll preview now has persisted statements, lines, liabilities, traces, Core record/packet commands, blocked payment/filing drafts, and Core `external_action.record` outcome proof, and AI-ops now has a deterministic Core `ai.infer` gateway with route, redaction, budget, inference, usage, audit, and evidence proof.
- Extend approved payroll handoffs beyond fail-closed internal application into scoped live credential checks, rollback paths, and dual-control execution workers before any submission or money movement.
- Extend customer-signal workflows, generated views, and eval fixtures beyond the seeded SatisfactionSignal, FeedbackItem, Complaint, Testimonial, and Review primitives; `customer_experience_operations` now has a first runtime `recovery.draft` slice plus `signals` view, and the remaining gates are escalation routing, promise follow-up, review-response drafting, approved send credentials, rollback proof, and receipt capture.
- Wire implementation handlers, retries, and generated views into the expanded operating workflow catalog; queued approval and packet handoffs now execute through `/workflow`.
- Extend adapter-intent, rule-change, capability-backed, approval-backed, and packet-backed workflow execution into additional domain-specific handlers; scheduled command drain now covers workflow steps, Revenue lead polling, and Revenue adapter retry/reconciliation.
- Extend Core adapter intent and rule-change workflow handlers into future worker contract fixtures.
- Extend Dispatch beyond the registered `schedule.propose`, `customer_update.draft`, `closeout.prepare`, and `exception.route` slices into scoped live credential checks and external execution gates; extend Finance beyond the registered `invoice.prepare`, `ar_followup.draft`, `cash_forecast.generate`, and `payment_draft.prepare` slices into live-accounting/payment readiness and dual-control execution gates; extend Workforce beyond the registered `hire.packet.prepare` and `payroll_input.prepare` slices into contractor packet, credential review, schedule readiness, and live HR/payroll credential gates; extend Compliance beyond the Core `obligation.scan`, registered `filing.prepare`, and compliance views into notice response, license renewal, evidence binder export, live agency credentials, broader rule-source coverage, and receipt capture while submission and legal advice stay blocked; extend Systems beyond its first dry-run/blocked runtime slice into scoped live credential gates; extend Customer Experience beyond `recovery.draft` into escalation, promise, review-response, and approved-send gates.
- Extend the Owner Chief-of-Staff Worker beyond approval/revision continuations into stale-source handling and broader factuality evals.
- Keep all worker-family HTTP controls on `/worker` with registered `worker`, `command` or `view`, command `idempotencyKey`, and explicit object `config` fields; do not add worker-family-specific URL or query shapes.
- Keep worker identities role-based, such as `revenue_operations` and `dispatch_operations`; do not reintroduce legacy family-worker identifiers, reserved route-noun roles such as `worker` or `api`, or `/api/*-worker`, `/api/*_worker`, or `/worker/<role>` naming in examples, docs, routes, scripts, or event namespaces.
- Keep control-plane token catalogs and durable credential inventory on exact route-qualified command lists; production auth must stay hash-only, catalog-backed, and fail closed without route, access, command, and operator identity scope. Stale managed token fingerprints are recovery-only for token rotation and credential upsert, and normal commands must keep rejecting them.
- Keep app-server/local worker mutation controls on the same explicit `command`, `worker`, `idempotencyKey`, and object `config` envelope as `POST /worker`, and keep app-server/local worker reads on `view`, `worker`, and read filters under `config`; do not add worker-family-specific package scripts, app-server tools, route-shaped command/view names, query fields, or top-level operator/context fields that bypass the command registry.
- Decide which worker-family read views should become shared Core reads as they repeat; Compliance `snapshot`, `obligations`, and `packet` are acceptable worker views for the first slice, but future shared ledgers should not drift into one-off view names.
- Clarify the Systems split before expanding it: `connector.health.scan`, `permission.review`, and `automation.plan` may belong partly in Core/platform primitives rather than staying only as Systems worker verbs.
- Separate shared control primitives from worker-business verbs as the registry matures; `approval.decide` and `continue` are still registered worker commands where they apply domain policy, but broad control flow should stay on `/approval`, `/workflow`, and Core lifecycle commands.
- Keep tightening permissive worker `configSchema.additionalProperties` settings command by command after direct fallback payloads have explicit extension fields; Revenue command configs now reject unknown top-level config fields on the public `/worker` surface, while some other early runtime configs still intentionally accept unknown nested config keys for compatibility.
- Keep the production object-storage Postgres backup timer healthy; `scripts/check-production-readiness.sh` now proves the timer, backup env, fresh local dump, and latest DigitalOcean Spaces backup object.
- Extend production readiness checks beyond the now-default-on customer-data gate into DigitalOcean managed-backup drift, firewall rule/tag drift, non-root default deploys, SSH hardening, signed/provenance-backed release artifacts, and recurring attestations.
- Keep the backup and observability timers installed with `READINESS_USER=continuous-deploy`, object-storage credentials, `REQUIRE_BACKUP_FRESH=true`, `CHECK_SYSTEMD_FAILED=true`, and a real HTTPS `ALERT_WEBHOOK_URL`; the disposable recovery drill is attested in production as `reports/recovery-drills/continuous-recovery-20260521T194000Z.md`, and strict production readiness must continue proving backup, observability, recovery-drill, non-root, token, credential, auth-audit, and live alert-webhook reachability evidence in the same run.
- Keep deploy-produced token rotation, control-plane credential inventory, disposable revocation drill, operator session review evidence, and `control_plane_auth_sessions` audit references current in readiness before broad customer use.
- Provision production scoped sender credentials and tested rollback playbooks, then run the first production controlled send through `/worker` `command=continue` with `config.execution`; CI now proves the Core `customer_message.send` boundary, explicit connection requirements, receipt recording, scope checks, rollback requirement, redacted credential hash, safe config persistence, and changed-input/unhashed replay conflict, while `/worker` `view: "readiness"` keeps live sender gates blocked until real credentials exist.
- Provision production payment-provider credentials, approval receipts, and rollback playbooks before enabling live provider payment-link creation; `payment_link.prepare` and payment-link approval continuation are runtime-covered now, but provider link creation and money movement are still blocked.
- Extend payment-link provider execution only after scoped payment-provider credentials, receipt capture, rollback proof, replay-conflict coverage, and production readiness gates exist; `config.execution` is intentionally rejected for payment-link continuations until that branch exists.
- Provision production inbox and CRM managed credential refs, create pollable active live-provider connections through `/core connection.upsert`, record readiness through `/core connection.health.record`, monitor scheduler coverage behind the persisted scheduler-owned connection-backed `lead.read` proof, and use `/worker` `view: "readiness"` as the Revenue dry-run launch report before any customer-data promotion.
- Continue Workforce runtime coverage beyond the first registered `/worker` slice; contractor packet, credential review, and schedule readiness remain follow-up commands, while hire packets, payroll-input readiness, restricted-document proof, and readiness views now run through the generic worker envelope with Core worker-run lifecycle and budget settlement.
- Continue Compliance runtime coverage beyond Core `obligation.scan` and the first registered `/worker` slice; `filing.prepare` now uses Core worker-run lifecycle and budget settlement while filing config stays in the worker payload, submission/legal advice remain blocked, and live agency credentials, rule-source breadth, and receipt capture are the next launch gates.
- Continue Dispatch runtime coverage beyond `schedule.propose` and `customer_update.draft` Core lifecycle proof; schedule proposals and customer updates now use the Core run row and Core budget settlement while source refs, constraints, message context, and policy remain under `/worker` `config`, and the next migration targets are `closeout.prepare` and `exception.route`.
- Keep worker contract metadata exposing the full contract catalog, runtime contracts, registered commands, non-executable follow-up commands, and queryable expansion catalog through `continuous.worker.schema`.
- Promote runner-built image deploys into a true registry digest lane; the GitHub DigitalOcean workflow now checks exact-SHA CI success and deploys checksum-verified runner-built app/migrate/scheduler images with `--no-build`, but it does not yet push immutable GHCR/DOCR digests.
- Use `docs/revenue-operations-worker-expansion.md` as the expansion gate list for the next worker iteration.
- Use `docs/revenue-operations-worker-v1-contract.md` as the machine-actionable contract for run and approval effects.
- Keep `docs/worker-readiness.md` and `docs/worker-handoffs.md` updated in the same change as any worker promotion or new cross-worker fixture; Dispatch now has schedule proposal, customer update draft, closeout packet, and exception routing proof, Finance now has invoice draft, AR follow-up, cash forecast, and payment draft cash-packet proof, Workforce now has hire packet and payroll-input readiness proof, Compliance now has generic `/worker` filing packet proof with Core worker-run and budget settlement, Customer Experience now has recovery draft and `signals` view proof, Growth now has `campaign.draft` and `campaigns` view proof, and the next launch slices are live credential gating plus dual-control/receipt execution readiness.
- Keep Core HTTP controls on `/core` with registered `command`, `core`, `idempotencyKey`, and `config` fields; do not add `/api/*` command paths or command-specific URLs.
- Keep Next MCP for Next.js diagnostics; keep app-server Core and worker tooling registry-backed, trusted-local, and free of production-token plumbing; CI now proves `continuous.worker.command` can execute real Revenue `lead.read`, `run`, `lead.classify`, `response.draft`, `quote.prepare`, `payment_link.prepare`, and `continue` for controlled-send and payment-link continuations, plus Owner, Dispatch, Finance payment-depth, Offer/Pricing, and Customer Experience recovery commands, and `POST /app-server` now provides the production-grade auth boundary for remote dynamic tool calls including Core command/view/schema calls, Core worker lifecycle commands, Core `obligation.scan`, Offer/Pricing `margin.review.prepare`, and Customer Experience `recovery.draft`.
- Remove the compatibility `/api/health` alias only after old probes and external monitors have moved to `/health`; remove the compatibility `GET /core?tenantSlug=...` summary read after clients use the `POST /core` view envelope; decide whether the `/approvals` UI route should stay as a human console while `/approval` remains the API surface.
- Keep the production app-server bridge on the generic `POST /app-server` dynamic-tool envelope, with worker commands shaped as `command`, `worker`, `idempotencyKey`, and `config` under `arguments`, and worker reads shaped as `view`, `worker`, and `config`; workflow and approval bridge tools now use the same selector/config rule with `workflow` or `approval` selectors under `arguments`, while deploy smoke now proves the bridge requires auth, exposes schema discovery, executes Core worker lifecycle commands, and executes Revenue `lead.read -> run` through the authenticated dynamic-tool bridge.
- Keep normal DigitalOcean deploys non-root by default, then make the production-readiness gate mandatory for customer-data deploys; strict workflow runs now fail before sync if `DEPLOY_USER` resolves to root, backup/observability timer installers can delegate private readiness env access to the non-root deploy account, and strict alerting must keep using a reachable real HTTPS webhook rather than an empty or dummy timer value.
- Continue Systems Operations beyond the first runtime slice: keep controls on the generic `/worker` envelope, preserve dry-run repair planning and blocked permission/automation execution, and add scoped live connector credentials only with approval receipts and rollback proof.
- Continue DigitalOcean release gate hardening: runner-built release images are now the normal GitHub deploy path, and the remaining gaps are registry digests and carrying the same image archive/digest path into emergency/operator deploys.
- Continue Offer and Pricing beyond the first runtime slice: `margin.review.prepare` now consumes the `revenue.quote_to_pricing` fixture, starts and completes through Core `worker.run.start` / `worker.run.complete`, writes pricing review workflow/evidence/approval/generated-view proof, settles budget through the Core run row, and replays idempotently through the generic `/worker` envelope; next gates are price-change, discount-exception, stale-cost-source, live price-publish, and customer-send handoff proofs.
- Keep managed control-plane credential upsert policy coverage current as new routes, commands, and app-server bridge capabilities are added; `control_plane.credential.upsert` now rejects empty durable scopes and route/access/command/tenant/worker-role scope broadening before persistence.
